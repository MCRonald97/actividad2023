
/*


const peso = parseFloat(prompt("¿Cuál es su peso?"));
const altura = parseFloat(prompt("¿Cuál es su altura?"));

function calculo_corporal(peso, altura) {
  const imc = peso / (altura * altura);
  return imc.toFixed(2);
}

const imcCalculado = calculo_corporal(peso, altura);
alert(`Su Índice de Masa Corporal es: ${imcCalculado}`);